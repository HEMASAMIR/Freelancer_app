name: freelancer
description: "QuickIn is a comprehensive booking and property management platform."

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.10.8


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_screenutil: ^5.9.3
  lottie: ^3.3.2
  flutter_launcher_icons: ^0.13.1
  dartz: ^0.10.1
  flutter_bloc: ^9.1.1
  bloc: ^9.2.0
  supabase: ^2.10.2
  supabase_flutter: ^2.3.4
  dio: ^5.9.1
  pretty_dio_logger: ^1.4.0
  get_it: ^7.7.0
  equatable: ^2.0.8
  google_sign_in: ^6.2.1
  url_launcher: ^6.3.2
  sign_in_with_apple: ^6.1.1
  crypto: ^3.0.3
  intl: ^0.20.2
  shimmer: ^3.0.0
  shared_preferences: ^2.2.2
  qr_flutter: ^4.1.0
  dotted_border: ^3.1.0
  image_picker: ^1.2.1
  share_plus: ^13.0.0
  cached_network_image: ^3.4.1
  flutter_local_notifications: ^18.0.1
  flutter_foreground_task: ^8.15.0
  connectivity_plus: ^6.1.1
  change_app_package_name: ^1.5.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  flutter_staggered_animations: ^1.1.1
  image: ^4.8.0
  flutter_tts: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  integration_test:
    sdk: flutter
  bloc_test: ^10.0.0


  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.7


flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/lottie/
    - assets/config/
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo_adaptive_foreground_small.png"
  min_sdk_android: 21
  adaptive_icon_background: "#F7F3F0"
  adaptive_icon_foreground: "assets/images/logo_adaptive_foreground_small.png"
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_native_splash:
  color: "#F7F3F0"
  image: "assets/images/splash.png"
  android_12:
    image: "assets/images/splash.png"
    color: "#F7F3F0"
